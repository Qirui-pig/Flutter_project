import 'package:flutter/material.dart';

class YunPage extends StatelessWidget {
  static final String routeName = '/yun';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('云村'),
      ),
    );
  }
}
