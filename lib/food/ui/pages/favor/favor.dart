import 'package:flutter/material.dart';

class FavorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favor'),
      ),
      body: Center(
        child: Text('is my favor'),
      ),
    );
  }
}
